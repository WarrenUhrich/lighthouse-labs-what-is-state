import React from 'react';

/**
 * A component is just a JavaScript function!
 * It must return JSX!
 * By convention we use PascalCase (instead of camelCase.)
 */

const HelloWorld = () => {
    return (
        <p>Hello, World!</p>
    );
};

export default HelloWorld;
